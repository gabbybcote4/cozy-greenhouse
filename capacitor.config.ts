import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bhadfrenchie.cozygreenhouse',
  appName: 'CozyGreenhouse',
  webDir: 'dist'
};

export default config;
